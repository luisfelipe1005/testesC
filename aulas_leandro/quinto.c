#include <stdio.h>

int main(void)
{
    float q = 0, n;
    printf("digite o  numero que deseja ver o quadrado \n");
    scanf("%f", &n);
    q = n * n;

    printf("o numero ao quadrado é: %f\n", q);

    return 0; // retorna a merda da função
}